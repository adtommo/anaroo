[build]
# Railway detects Dockerfile per service — no global build needed

[deploy]
# Healthcheck
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
